{
  "name": "college-lost-and-found-backend",
  "version": "1.0.0",
  "description": "Backend for College Lost and Found System",
  "main": "server.js",
  "scripts": {
    "postinstall": "cd client && npm install && npm run build",
    "build": "cd client && npm install && npm run build",
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "keywords": [
    "college",
    "lost-and-found",
    "mongodb",
    "express",
    "react"
  ],
  "author": "Your Name",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongoose": "^8.0.3",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
